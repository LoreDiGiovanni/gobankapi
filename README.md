# A simple api without framework in go

## Store interface 
The Store interface (interface for db) is generale bust in this case i have implemented a
[postgres docker](https://hub.docker.com/_/postgres)
